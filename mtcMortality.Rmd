---
title: "NOAC network meta-analysis: Mortality"
author: "Benjamin Chan"
date: "`r Sys.time()`"
output: 
  html_document: 
    keep_md: yes
    toc: yes
---

[Back](README.md) to main page.

Clean up the data (do not show the code).

```{r, echo=FALSE, results='asis'}
D1 <- readSheet("Mortality")
D1 <- tidyData(D1)
write.csv(D1, file="mtcMortalityData.csv", row.names=FALSE)
print(xtable(D1, digits=rep(0, 5)), type="html", include.rownames=FALSE)
network <- mtc.network(D1)
```

Run the model using `r sprintf("%s-effects", effect)`.

```{r network, results='hide'}
M <- mtc.model(network, type="consistency", linearModel=effect)
plot(M)
results <- mtc.run(M, n.adapt=nAdapt, n.iter=nIter, thin=thin)
```

# Summary

Direct and indirect odds ratios and 95% confidence bounds are stored in
[mtcMortalityOddsRatios.csv](mtcMortalityOddsRatios.csv).

```{r, results='asis'}
or <- combineResults()
write.csv(or, file="mtcMortalityOddsRatios.csv", row.names=FALSE)
print(xtable(or), type="html", include.rownames=FALSE)
```

# Forest plots, NOAC vs NOAC

```{r, child="forestPlots.Rmd"}
```

# Diagnostics

```{r, child="modelDiagnostics.Rmd"}
```
