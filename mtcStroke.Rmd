---
title: "NOAC network meta-analysis: Stroke"
author: "Benjamin Chan"
date: "`r Sys.time()`"
output: 
  html_document: 
    keep_md: yes
    toc: yes
---


Clean up the data (do not show the code).

```{r, echo=FALSE}
D2 <- readSheet("Stroke")
D2 <- tidyData(D2)
write.csv(D2, file="mtcStrokeData.csv", row.names=FALSE)
show(D2)
network <- mtc.network(D2)
```

Run the model using `r sprintf("%s-effects", effect)`.

```{r strokeNetwork, results='hide'}
M <- mtc.model(network, type="consistency", linearModel=effect)
plot(M)
results <- mtc.run(M, n.adapt=nAdapt, n.iter=nIter, thin=thin)
```

# Summary

Direct and indirect odds ratios and 95% confidence bounds are stored in
[mtcStrokeOddsRatios.csv](mtcStrokeOddsRatios.csv).

```{r}
or <- combineResults()
write.csv(or, file="mtcStrokeOddsRatios.csv", row.names=FALSE)
show(or)
```

# Diagnostics

```{r, child="modelDiagnostics.Rmd"}
```
